# Nyayalay Frontend

React + TypeScript + Vite frontend.

## Run

```powershell
npm install
npm run dev
```

The API must be running at:

```text
http://127.0.0.1:8000
```

Current UI is intentionally small. Build the rest of the product only after the API contract is stable.
